# 변경 이력

이 저장소의 릴리스 이력. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 참고하되,
검증되지 않은 항목은 "미검증"으로 명시한다 — 근거 없는 완료 표시를 하지 않는다.

## [v0.1.4] - 2026-09-19

쿠폰 칸 경계를 카드 크기에 맞게 좁힌다. 실제 앱에서 공유로 넣는 흐름까지 확인했다.

### 다운로드

| 파일 | SHA-256 |
| --- | --- |
| `CouponIt-v0.1.4-debug.apk` | `09243ad5b696d1423363d672bf7b1b8140c0f05fc93767842232f2dee2339458` |

디버그 서명이다. 이전 디버그 APK 위에 덮어 설치된다.

### 고침

- 첫 칸에 페이지 머리말(`교환권 전체보기`, `총 5장의 교환권이 있습니다.`)이, 마지막 칸에 하단 버튼(`주문내역 가기`)이
  쿠폰 영역으로 섞여 목록 썸네일에 보이던 문제. 바코드에서 위아래로 줄을 훑다가 줄 간격이 갑자기 벌어지면 끊는다.
  잘린 카드의 브라우저 주소창·상태바도 함께 빠진다.
- 바코드에 가장 가까운 줄은 떨어져 있어도 반드시 포함해, 경계를 좁히다가 상품명이나 유효기간을 잃지 않는다.
  끊을 자리를 못 찾으면 이전처럼 바코드 간격으로 잡은 칸을 그대로 쓴다.

### 검증됨

- JVM 단위 테스트 57개 통과(칸 분리 12개). Android Lint 오류 0개.
- 계측 테스트 12개 통과, 2개는 개인 이미지 인자가 없어 건너뜀.
- 실제 이미지 칸 경계: 1번 화면 `514..1012`, `1068..1568` / 2번 화면 `206..643`, `704..1204`, `1258..1763`.
  다섯 칸 모두 사용처·종료일이 붙고 재인식 결과가 가져오기와 같다.
- **실제 앱 확인**: 에뮬레이터에 공유로 두 장을 넣어 쿠폰 5개가 추가되고(`사용 전 6`, `정보 확인 0`),
  DB는 쿠폰 6개·원본 3장·서로 다른 번호 6개. 같은 두 장을 다시 넣어도 쿠폰과 원본 수가 그대로였다.
  근거: `docs/verification/MULTI_COUPON_VERIFICATION.md`

## [v0.1.3] - 2026-09-19

스크린샷 한 장에 쿠폰이 여러 개면 바코드별로 나눠 저장한다. DB 스키마가 v2로 올라가며 기존 데이터는 마이그레이션된다.

### 다운로드

| 파일 | SHA-256 |
| --- | --- |
| `CouponIt-v0.1.3-debug.apk` | `425e8c4dbc247dd12aadd40a3dcd1e8d77b2e7766e9f79752abb4747ad5278b5` |

디버그 서명이다. 이전 디버그 APK 위에 덮어 설치된다.

### 추가됨

- **여러 쿠폰 분리**: 이미지의 바코드를 끝까지 찾고, 바코드 사이 간격에서 칸 경계를 잡아 쿠폰을 각각 저장한다.
  같은 높이로 자르지 않으며, 바코드가 없는 잘린 카드는 쿠폰으로 만들지 않는다.
- **원본 공유**: 스크린샷은 한 번만 저장하고 쿠폰마다 `assetId`와 영역을 가리킨다. 목록·수정은 그 칸만,
  코드 화면은 쿠폰 영역·바코드 확대·전체 스크린샷을 함께 보여준다.
- **중복 방지**: 같은 쿠폰 번호가 이미 있으면 건너뛴다. 같은 화면을 다시 담아도 쿠폰이 늘지 않는다.
- **원자적 저장**: 원본·쿠폰·바코드 후보를 한 트랜잭션으로 쓴다. 하나도 저장되지 않으면 사본 파일도 남기지 않는다.
- 가져오기 결과 안내에 찾은 수·저장 수·중복 수·확인 필요 수를 구분해 표시한다.
- 네이버페이 교환권 화면 형식 인식: `[EVENT]` 태그는 사용처가 아니라 행사 표시로 분리하고,
  `메가MGC커피 선물하기`처럼 브랜드 뒤 안내 문구를 떼며, 라벨 없는 `2026.12.21까지 사용가능`을 종료일로 읽는다.
  `93일남음` 같은 상대 기간은 절대 날짜를 대신하지 않는다.
- DB v2 마이그레이션(`MIGRATION_1_2`)과 마이그레이션 테스트.

### 고침

- 상세 화면 재인식이 잘라낸 이미지를 새로 읽어 가져올 때와 결과가 달랐다(첫 칸의 종료일 누락, `[EVENT]`의 여는 대괄호 손실).
  이제 원본을 읽고 좌표로 걸러 가져오기와 같은 결과를 낸다.
- OCR이 숫자 사이에 공백을 넣어 읽은 날짜(`2026.1 2.21`)를 놓치던 문제.
- 가져오기 중 화면을 벗어나도 중복 실행되지 않도록 막았다.

### 검증됨

- JVM 단위 테스트 55개 통과(칸 분리 10개, 파서 23개 포함). Android Lint 오류 0개, 경고 10개는 의존성 업데이트 권고.
- 에뮬레이터(API 36) 계측 테스트 12개 통과, 2개는 개인 이미지 인자가 없어 건너뜀.
  실제 교환권 화면 2장에서 **쿠폰 5개**(2개 + 3개)가 만들어지고, 사용처·종료일이 각 칸에 연결되며,
  다시 담으면 저장 0개·중복 5개로 늘지 않음을 확인했다. 근거: `docs/verification/MULTI_COUPON_VERIFICATION.md`
- v1 데이터가 있는 설치본에 덮어 설치 후 기존 쿠폰이 유지되는지 실기기(에뮬레이터)에서 확인했다.

### 미검증

- 2열 배치처럼 좌우로 나뉜 쿠폰 화면, 한 카드에 바코드가 둘인 경우(확인 필요로 표시만 함)
- 상품명의 OCR 한 글자 오인식은 보정하지 않는다(사용처만 브랜드 사전으로 보정)

## [v0.1.2] - 2026-09-14

화면 이동과 조작 사용성 개선. 데이터 스키마 변경은 없다.

### 다운로드

| 파일 | SHA-256 |
| --- | --- |
| `CouponIt-v0.1.2-debug.apk` | `67ceb5b9736cae0161725f556e13318baf39c2bb4169add99fce4bfe259cf723` |

디버그 서명이다. 이전 디버그 APK 위에 덮어 설치된다.

### 바뀜

- **뒤로가기**: 수정·코드 제시·보관함·설정 화면에서 앱을 나가지 않고 이전 화면으로 돌아간다. 홈에서는 검색어·사용처 필터를 먼저 해제하고, 그다음 뒤로가기에서 앱을 나간다.
- **쿠폰 카드**: 카드 어디를 눌러도 정보 수정 화면이 열린다. **원본·바코드** 버튼만 코드 제시 화면을 연다. 전에는 이미지를 누르면 코드 화면이 열렸다.
- **코드 제시 화면**: **닫기**와 뒤로가기가 열었던 화면으로 돌아간다. 홈에서 열면 홈, 수정 화면에서 열면 수정 화면이다. 전에는 항상 수정 화면으로 갔다.
- **수정 화면**: 상단 버튼을 **← 뒤로**로 바꾸고 열었던 화면으로 돌아간다. 저장 후에도 열었던 화면으로 돌아간다. 보관함에서 열었으면 보관함이다.
- **종료일 입력**: `2026-12-09`, `2026.12.09`, `2026/12/9`, `2026년 12월 9일`, `20261209`를 받는다. 숫자 키패드를 띄운다.

### 추가됨

- 저장하지 않은 변경이 있을 때 뒤로가기·**← 뒤로**·보관·코드 화면 이동 전에 확인 창을 띄운다(**계속 수정** / **저장하지 않고 나가기**).
- **사용했어요**를 누르면 확인 창을 띄운 뒤 기록한다.
- 보관함 카드를 누르면 정보 수정 화면이 열린다. 사용 완료·보관 상태를 카드에 표시한다.

### 고침

- 보관함의 **복원**이 사용 완료 쿠폰에는 아무 변화가 없던 문제. 이제 지갑으로 옮기면서 마지막 사용 기록을 되돌리는 이벤트를 남기고 사용 전 상태로 돌린다.
- 금액권 코드 제시 화면의 "사용 금액 기록은 상세에서" 버튼이 실제로는 사용 완료로 기록하던 문제. 이제 수정 화면의 금액 기록으로 이동한다.
- 사용 완료된 쿠폰의 수정 화면에 **사용했어요** 대신 상태 안내와 **지갑으로 복원**을 보인다.

### 검증됨

- JVM 단위 테스트 40개 통과(화면 뒤로가기 목적지 4개, 날짜 입력 2개 추가). Android Lint 오류 0개, 경고 9개는 의존성 업데이트 권고.
- 에뮬레이터(API 36) 계측 테스트 `WalletNavigationUiTest` 4개 통과. 메모리 DB만 사용한다.
  - 카드 탭 → 수정 화면 → 뒤로가기 → 홈
  - 원본·바코드 → 코드 화면 → 닫기 → 홈, 다시 열고 뒤로가기 → 홈
  - 변경 후 뒤로가기 → 확인 창 → 계속 수정 유지, **← 뒤로** → 저장하지 않고 나가기 → DB 불변
  - 사용했어요 → 취소 시 미기록, 확인 시 완료 → 보관함 복원 → 사용 전·지갑
- 기존 OCR·원본 표시 계측 테스트 4개도 함께 통과. 실제 쿠폰 이미지가 필요한 2개는 인자가 없어 건너뜀.
- 에뮬레이터 앱에서 직접 확인: 칩 변경 후 시스템 뒤로가기에서 확인 창이 떴다. 글자 칸 입력 중에는 첫 뒤로가기가 키보드를 닫고 두 번째에 확인 창이 떴다. 저장하지 않고 나가기 후 기존 쿠폰 정보는 바뀌지 않았다.

### 미검증

- 실기기의 제스처 뒤로가기(에뮬레이터 키 이벤트로만 확인)
- 큰 글자 설정(1열 목록)에서 카드 탭 영역

## [v0.1.1] - 2026-09-14

사용 여부를 발행사 조회 페이지에서 직접 확인하는 흐름 추가와, 11번가 메가MGC커피 교환권 정보 인식 수정.

### 다운로드

| 파일 | SHA-256 |
| --- | --- |
| `CouponIt-v0.1.1-debug.apk` | `2221848235129fd0387d33707d3e560ec944065143e1a75622fcc351cbc55040` |

디버그 서명이다. v0.1.0 디버그 APK와 같은 서명이면 덮어 설치된다.

### 추가됨

- 상세 화면에 **발행사** 선택(쿠프마케팅·기프티쇼·페이즈)과 **발행사에서 사용 여부 조회** 버튼
  - 쿠폰 번호를 클립보드에 복사(Android 13+는 민감 정보로 표시)한 뒤 발행사 조회 페이지를 연다.
  - 앱이 사용 여부를 자동 조회하지 않는다. 조회 페이지(대부분 휴대폰 인증 필요)에서 확인한 뒤 **사용했어요**로 직접 기록한다.
  - 사용처가 메가MGC커피면 쿠프마케팅을 추천한다(메가MGC커피 공식 FAQ: 앱 미등록 모바일상품권은 쿠프마케팅 문의). 원본 표기가 다르면 사용자가 바꾼다.
  - 발행사는 **정보 저장**을 눌러야 저장된다. 기존 `issuerName` 필드를 사용해 DB 스키마 변경은 없다.
- 실제 쿠폰 이미지 가져오기를 단계별로 확인하는 진단 계측 테스트 `CouponImportDiagnosticTest` (이미지 경로 인자를 줄 때만 실행, 쿠폰 번호는 로그에서 가림)

### 고침

- 강조 배경 때문에 OCR이 브랜드명을 한 글자 잘못 읽으면(`메가MGC커파`) 사용처를 찾지 못하던 문제. 5글자 이상 브랜드만 한 글자 차이까지 허용한다.
- `[2609, 11번가]`처럼 한 줄 전체가 괄호인 판매처 태그를 상품명으로 잡을 수 있던 문제.

### 검증됨

- JVM 단위 테스트 34개 통과(발행사 4개, 파서 회귀 3개 추가). Android Lint 오류 0개, 경고 9개는 의존성 업데이트 권고.
- 에뮬레이터(API 36)에서 실제 메가MGC커피 교환권 이미지 가져오기 → 상품명 `(ICE)하우스밀크 라떼`, 사용처 `메가MGC커피`, 종료일 `2026-12-09`, 정보 확인 필요 해제. 근거: `docs/verification/OCR_VERIFICATION.md`
- 에뮬레이터 상세 화면에서 발행사 미선택 시 조회 버튼 비활성, 쿠프마케팅 선택 후 조회 시 `https://cs.inumber.co.kr/...` VIEW 인텐트로 Chrome 실행, 저장 없이 나가면 발행사가 저장되지 않음을 확인.

### 미검증

- 발행사 조회 페이지에서 실제 사용 여부 확인(휴대폰 인증 필요)
- 클립보드에 복사된 번호의 기기 내 확인, 사용처 추천 칩의 기기 표시(한글 입력 자동화 제약으로 단위 테스트로만 확인)
- 발행사 저장 후 재진입 표시(사용자 기존 쿠폰 데이터를 바꾸지 않으려고 에뮬레이터에서 저장하지 않음)

## [v0.1.0] - 2026-09-13

이 저장소(`AIProject-k/CouponIt`)의 최초 배포. 기획 v2.0(`CouponIt_자체쿠폰지갑_상세기획_개발계획_v2.0.md`)의
P0/P1 범위를 Kotlin 네이티브 Android 단일 모듈로 구현한 개발 검증 APK다. 서버·계정 없이 기기 내부에서만 동작한다.

### 다운로드

[릴리스 페이지](https://github.com/AIProject-k/CouponIt/releases/tag/v0.1.0)에 디버그 APK를 첨부했다.

| 파일 | SHA-256 |
| --- | --- |
| `CouponIt-v0.1.0-debug.apk` | `753bcab44b0490ff410b67d3002c8ac6942a57cd13f0fc7e03d19fef7168c6bd` |

서명은 디버그 키다. 배포용 서명이 아니므로 개인 테스트 기기 설치 용도로만 쓴다. 다른 서명의 기존 설치가 있으면
먼저 제거해야 설치된다. 체크섬은 `SHA256SUMS.txt`(release 자산)에도 함께 올려뒀다.

### 추가됨

**가져오기 · 저장**
- Android Photo Picker로 이미지 최대 30장 선택, 다른 앱의 JPEG/PNG 공유 Intent 수신
- 선택한 원본을 앱 전용 저장소에 먼저 복사 후 SHA-256 해시·크기·MIME·해상도 기록 (`ImportPolicy`, `CouponImporter`)

**인식**
- ML Kit 온디바이스 바코드 인식으로 후보와 원본 좌표 보존 (`BarcodeRecognizer`)
- ML Kit 한국어 OCR(`text-recognition-korean:16.0.1`)로 상품명·사용처·유효기간 자동 추출 (`CouponTextRecognizer`, `CouponTextParser`)
  - 라벨(사용처/교환처/브랜드, 상품명/교환상품, 유효기간 계열) 기반 줄 파싱
  - 날짜는 명확한 라벨·형식(점/하이픈/슬래시/한국어, 범위 종료일)에서만 확정 — 모호하면 미확정으로 남김
  - 상세 화면 재인식은 **빈 항목만** 채움, "정보 저장"을 눌러야 DB에 반영

**화면**
- 홈 요약(미사용·임박·확인 필요)·검색·사용처 필터·임박순 정렬·2열/목록/큰 글자 1열 보기
- 상세 편집, 원본 이미지 보기, 검출 영역 확대 제시(화면 밝기·화면 유지 후 원복)
- 보관함 이동/복원, 교환권 사용 처리, 금액권 잔액·사용 금액 기록

**저장소**
- Room 스키마: 쿠폰·원본 자산·코드 후보·사용 이벤트·제시 세션
- 코드 제시 화면을 닫는 것만으로 사용 완료 처리되지 않는 분리된 흐름

### 검증됨 (근거: `docs/verification/`)

- JVM 단위 테스트 27개 통과 (`./gradlew testDebugUnitTest`)
- Android Lint 오류 0개
- 에뮬레이터(API 36) 설치·실행·핵심 UI 확인
- 실제 사용자 제공 쿠폰 이미지 1장으로 바코드+OCR 동시 인식, 저장, 재인식 회귀 확인
- Android 계측 테스트 8개(OCR 3개 + 실사진 회귀 5개) 통과

### 아직 없음 / 미검증

- 다중 코드 선택 UI, 사용 이벤트 이력 목록과 개별 취소
- 만료 알림, 앱 바로가기, 사용자 설정(DataStore)
- 암호화 백업·복원
- 실사용 쿠폰 이미지 10~20장 규모의 인식률 측정, 타 기기 화면 재촬영 인식(V2), 매장 실제 승인(V3)

### 알려진 환경 이슈

- Android Studio 번들 JBR가 JDK 25로 갱신되면 Gradle 8.13 내장 Kotlin DSL 컴파일러가 버전 문자열을 파싱하지
  못해 빌드가 실패한다. `JAVA_HOME`을 JDK 17/21(예: Zulu)로 지정해 실행한다. 코드 결함이 아니라 툴체인 조합 문제다.

### 참고

- 이 코드는 이전 프로젝트(`OptionFit`, 차량 옵션 비교 웹앱)와 같은 로컬 작업 폴더를 재사용해 작성됐다.
  이 저장소의 이력은 CouponIt으로 전환한 시점부터 새로 구성했고, OptionFit 쪽 커밋·파일은 포함하지 않는다.

[v0.1.4]: https://github.com/AIProject-k/CouponIt/releases/tag/v0.1.4
[v0.1.3]: https://github.com/AIProject-k/CouponIt/releases/tag/v0.1.3
[v0.1.2]: https://github.com/AIProject-k/CouponIt/releases/tag/v0.1.2
[v0.1.1]: https://github.com/AIProject-k/CouponIt/releases/tag/v0.1.1
[v0.1.0]: https://github.com/AIProject-k/CouponIt/releases/tag/v0.1.0
